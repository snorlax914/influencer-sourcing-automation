# 인플루언서 발굴 · 판정 · 스코어링 보고서

부산 외식업체를 위한 인스타그램 인플루언서(한국·특히 부산을 방문/거주하는 외국인,
음식 콘텐츠 중심) 발굴 파이프라인 요약.

## 전체 구조

```
발굴(크롤링) ──> 1차 선별(LLM) ──> 2차 스코어링(코드) ──> 등급/컨택
 discover.py       judge.py          judge.py
      └─────────── 웹 대시보드(webapp.py)로 실행·모니터링 ───────────┘
```

- 각 단계는 분리되어 있고 산출물도 따로 쌓인다(`candidates.csv` / `verdicts.csv`).
  루브릭·배점을 바꿔도 재크롤 없이 다시 돌릴 수 있다.
- 로컬 Ollama 연동으로 판정 비용 없이 돌릴 수 있다(`.env`의 `OLLAMA_HOST`).

---

## 1. 인플루언서 발굴 (`discover.py`)

인스타그램을 크롤링해 후보 계정을 모으는 단계.

- **로그인**: 브라우저 세션 쿠키 재사용(`IG_SESSIONID` 또는 크롬 쿠키). 자동화 차단을 피함.
- **후보 수집 경로**: 시드 계정 목록(seeds) + 특정 계정의 팔로잉 확장.
- **차단 회피**: 계정마다 요청 간 대기(기본 6초), 재시도 제한.
- **수집 항목**(`candidates.csv`):

| 필드 | 설명 |
|---|---|
| username / full_name | 계정명 · 표시 이름 |
| followers / posts | 팔로워 수 · 게시물 수 |
| hangul_ratio | 소개글·이름의 한글 비율(외국인 신호) |
| biography / external_url / email | 소개글 · 외부 링크 · 이메일 |
| captions | 최근 게시물 캡션(기본 3개) |

> ⚠️ 좋아요 수·게시물 날짜·위치태그는 수집하지 않음(요청량·차단 위험). 이는 스코어링 범위에 영향(6절).

---

## 2. 1차 선별 — LLM 판정 (`judge.py`)

스코어링 전에 **명백히 무관한 계정을 걸러내는** 단계. 점수는 매기지 않고 **합격/불합격**만 판정한다.

- **모델**: 로컬 Ollama(`qwen3`, `exaone3.5` 등) / Claude / OpenAI / Gemini 선택.
- **판정 성격**: 발굴 단계라 재현율 우선 — **애매하면 통과**.
- **불합격 기준**(명백할 때만): 상업·브랜드 계정, 한국·음식과 접점 전혀 없음, 정보가 텅 빔.
- 합격한 계정만 2차 스코어링으로 넘어가므로, 계정당 LLM 호출은 **선별 1회**뿐이다.

---

## 3. 2차 스코어링 (`judge.py`)

**LLM 없이 크롤 텍스트(캡션·소개글) + 릴스 조회수로 정량 계산.** 값이 흔들리지 않고 근거가 추적된다.

축은 넷이다: **① 도달(릴스 조회수) · ② 지역 · ③ 외국인 · ④ 음식.**

### 3.1 축별 배분 — 도달이 가장 무겁다

| 티어 | **도달** | 지역 | 외국인 | 음식 | 합 |
|---|---|---|---|---|---|
| 소형 | **40** | 25 | 18 | 17 | 100 |
| 중형 | **45** | 24 | 17 | 14 | 100 |
| 대형 | **50** | 24 | 15 | 11 | 100 |

**도달이 점수의 40~50%다.** 나머지 세 축을 다 합쳐도 도달 하나를 겨우 넘는다.
지역·외국인·음식은 "이 계정이 우리와 맞는가"를 보지만, 도달은 "실제로 몇 명에게 닿는가"를 본다.
맞는 계정을 찾아도 아무도 안 보면 소용이 없으므로 광고 효과와 가장 직접 연결되는 축에 배점을 몰았다.
단일 최대 축이라는 것이 티어마다 분명하다 — 다음으로 큰 축은 소형 부산 22 · 중형 부산 18 · 대형 한국 13.

### 3.2 티어 (팔로워 규모)와 만점

| 티어 | 팔로워 | 만점(조회수 없음) | 만점(조회수 있음) |
|---|---|---|---|
| 소형 | ~1만 미만 | 60 | 100 |
| 중형 | 1만 ~ 10만 | 55 | 100 |
| 대형 | 10만 이상 | 50 | 100 |

> 최종점수 = `반올림(원점수 × 100 ÷ 티어 만점)`

도달 축은 **릴스 조회수를 추출한 계정에만** 붙는다(조회수는 로그인한 브라우저 화면에만 뜨므로
계정마다 따로 수집한다 — 4절 참고). 조회수를 아직 안 뽑은 계정은 **도달 0점이 아니라 축 자체를 뺀다**
(= 도달을 '평균'으로 가정). 수동 추출을 안 했다는 이유로 순위가 밀리면 안 되기 때문이다.

### 3.3 미측정 계정의 등급 상한 (A는 재본 계정에만)

도달이 점수의 절반을 차지하는 이상, 미측정 계정의 점수는 **절반이 가정값**이다.
그 상태로 A(즉시 컨택)를 주면 실제로는 도달이 바닥인 계정에 사람을 보내게 된다.

> **조회수 미측정 계정은 최고 B등급까지만 받는다**(`UNMEASURED_GRADE_CAP`).

순위에는 그대로 남지만 '즉시 컨택' 판정은 실제로 재본 계정에만 붙는다. 대시보드에서는 등급 배지 옆에
`↑A?` 표시가 붙어, 조회수를 뽑으면 A로 올라갈 수 있는 계정을 바로 알아볼 수 있다 —
**이것이 조회수를 어느 계정부터 뽑을지 정하는 작업 순서가 된다.**

예: `skzkxk333`은 텍스트 축이 58/60이라 97점이지만 조회수 미측정이면 `B ↑A?`로 뜬다.
실제로 재보니 도달 31/40 → 89점 **A**.

### 3.4 지역 게이트 (통과 실패 시 최종 0점·D등급)

| 티어 | 통과 조건 |
|---|---|
| 소형 | 부산 콘텐츠 있음(필수) |
| 중형 | 부산 있음 **또는** 한국(부산 외) 10~30%↑ |
| 대형 | 부산 있음 **또는** 한국 콘텐츠 조금이라도 있음 |

### 3.5 배점표 (티어별)

**① 도달 · 조회수 배율** (= 릴스 조회수 중앙값 ÷ 팔로워) — 소형 26 · 중형 24 · 대형 22

'팔로워가 진짜인가, 알고리즘이 밀어주는가'를 본다. 팔로워를 사서 부풀린 계정은 규모가 커도
배율이 바닥이라 여기서 걸린다.

| 신호 | 소형 | 중형 | 대형 |
|---|---|---|---|
| 매우 높음 | 26 | 24 | 22 |
| 높음 | 19 | 18 | 17 |
| 보통 | 12 | 12 | 11 |
| 낮음 | 5 | 6 | 5 |
| 매우 낮음 | 0 | 0 | 0 |

**① 도달 · 절대 조회수** (릴스 조회수 중앙값) — 소형 14 · 중형 21 · 대형 28

실제로 몇 명에게 닿는가. 배점이 대형에 쏠린 건 대형 계정을 쓰는 이유가 곧 절대 도달이기 때문이다.
소형이 손해 보지는 않는다 — 소형이 3만을 찍으면 배율도 만점권이라 도달 축 전체가 만점이 된다.

| 신호 | 소형 | 중형 | 대형 |
|---|---|---|---|
| 10만+ | 14 | 21 | 28 |
| 3만~10만 | 14 | 19 | 23 |
| 1만~3만 | 12 | 15 | 17 |
| 3천~1만 | 7 | 8 | 9 |
| 3천 미만 | 0 | 0 | 0 |

**② 지역 · 부산 비율**

| 신호 | 소형 | 중형 | 대형 |
|---|---|---|---|
| 30%+ | 22 | 18 | 11 |
| 15~30% | 16 | 13 | 8 |
| 5~15% | 10 | 9 | 5 |
| 1~5% | 5 | 4 | 3 |
| 없음 | 0 | 0 | 0 |

**② 지역 · 한국(부산 외) 비율**

| 신호 | 소형 | 중형 | 대형 |
|---|---|---|---|
| 30%+ | 3 | 6 | 13 |
| 10~30% | 2 | 4 | 9 |
| 서울·소량 | 1 | 2 | 5 |
| 없음 | 0 | 0 | 0 |

**③ 외국인 · 캡션 언어**

| 신호 | 소형 | 중형 | 대형 |
|---|---|---|---|
| 외국어 | 11 | 11 | 9 |
| 혼용 | 7 | 7 | 5 |
| 한국어 | 0 | 0 | 0 |

**③ 외국인 · 바이오/계정명 언어**

| 신호 | 소형 | 중형 | 대형 |
|---|---|---|---|
| 외국어 | 7 | 6 | 6 |
| 혼용 | 4 | 3 | 3 |
| 한국어 | 0 | 0 | 0 |

**④ 음식 비율**

| 신호 | 소형 | 중형 | 대형 |
|---|---|---|---|
| 50%+ | 17 | 14 | 11 |
| 30~50% | 13 | 11 | 8 |
| 15~30% | 9 | 7 | 6 |
| 5~15% | 4 | 4 | 3 |
| 5% 미만 | 0 | 0 | 0 |

### 3.6 신호 추출 방식

| 신호 | 계산 방법 | 버킷 경계 |
|---|---|---|
| 언어(캡션·바이오) | 한글 비율 `h` | `h≥0.7`→한국어 · `h≤0.2`→외국어 · 그 외→혼용 |
| 부산 비율 | 부산 지명 키워드 걸린 캡션 비율 | ≥30/≥15/≥5/>0/0 → over30/b15_30/b5_15/b1_5/zero |
| 한국 비율 | 한국(부산 외) 키워드 걸린 캡션 비율 | ≥30/≥10/>0/0 → over30/b10_30/seoul_small/none |
| 음식 비율 | 음식 키워드 걸린 캡션 비율 | >50/≥30/≥15/≥5/<5 → over50/r30_50/r15_30/r5_15/under5 |
| 조회수 배율 | 중앙값 ÷ 팔로워 | **티어마다 다름**(아래) |
| 절대 조회수 | 중앙값 | ≥10만/≥3만/≥1만/≥3천/그 미만 |

**조회수 배율의 구간 경계** — 같은 1.0배라도 팔로워 3천 계정에선 평범하고 10만 계정에선 대단한
수치다. 경계를 하나로 두면 대형은 전부 바닥 구간에, 소형은 전부 상위 구간에 몰려 축이 순위를
못 가른다. 그래서 경계만 티어별로 둔다(릴스는 비팔로워에게도 뿌려지므로 배율이 1을 넘는 것이
이상하지 않다 — 좋아요 ER과 기준선이 다른 이유).

| 티어 | 매우 높음 | 높음 | 보통 | 낮음 |
|---|---|---|---|---|
| 소형 | ≥3.0배 | ≥1.5 | ≥0.7 | ≥0.3 |
| 중형 | ≥1.5배 | ≥0.8 | ≥0.4 | ≥0.15 |
| 대형 | ≥0.8배 | ≥0.4 | ≥0.2 | ≥0.08 |

> - **바이오 거주 보정**: 소개글에 부산 언급이 있으면 캡션에 지명이 없어도 최소 신호로 인정.
> - **키워드**: `BUSAN_KEYWORDS`/`KOREA_KEYWORDS`/`FOOD_KEYWORDS`(한·일·영·중 + 이모지), `judge.py`에서 튜닝.
> - **릴스 표본 하한(3개)**: 릴스가 3개 미만인 계정은 도달 축을 채점하지 않는다. 1~2개의 중앙값은
>   평소 성과가 아니라 그날의 운에 가깝고, 그걸로 40~50점을 움직이면 표본 적은 계정만 위아래로 튄다.
> - **고정(📌) 릴스 제외**: 계정이 박아둔 대표작이라 평소 성과가 아니고 보통 평소의 10배씩 나온다.
>   몇 개만 섞여도 중앙값이 자릿수째 달라져 통계에서 뺀다(`reels.py`).

### 3.7 등급

| 등급 | 점수 | 액션 |
|---|---|---|
| A | 70+ | 즉시 컨택 (**조회수를 잰 계정만**) |
| B | 50~69 | 컨택 후보 |
| C | 35~49 | 예비 풀 |
| D | 35 미만 · 게이트 탈락 | 보류 |

> 조회수 미측정 계정은 점수가 70을 넘어도 B로 제한된다(3.3절). 대시보드에 `B ↑A?`로 뜬다.

---

## 4. 웹 대시보드 (`webapp.py`)

- 발굴·판정을 버튼으로 실행하고 진행 로그를 실시간(SSE)으로 확인.
- 후보 목록 테이블: **선별(합격/불합격) · 점수 · 등급 · 릴스 조회수 · 1차 판정 이유** 표시.
- 계정 행을 **펼치면 계정별 채점 표**(차원별 배점·비중 막대)와 **환산 점수 미터 · 채점 이유 칩**을 우측 섹션에 시각화.
- 판정 중 진행 바 + 방금 채점된 행 플래시 등 UX 처리.
- **릴스 조회수 워크플로**: 계정마다 [조회수 추출] 버튼 → 서버 큐 적재 → Claude Code가
  Claude for Chrome으로 사용자의 로그인된 크롬에서 추출 → 결과 저장 시 **그 계정을 자동 재채점**
  (LLM 호출 없음). 등급 배지의 `↑A?` 표시가 어느 계정부터 뽑을지 알려준다.

---

## 5. 채점 예시

**도달이 점수를 끌어올린 경우** — `chuchuperoaichu`(부산 거주 일본인, 팔로워 9,157 → 소형):

```
조회수 추출 전:
[소형·조회수 미측정] 부산 22 + 한국 0 + 캡션 0 + 바이오 4 + 음식 0 = 26/60 → 43점 (C)

조회수 추출 후(릴스 12개 중앙값 11,000회 · 팔로워 대비 1.20배):
[소형] 부산 22 + 한국 0 + 캡션 0 + 바이오 4 + 음식 0 + 배율 12 + 도달 12 = 50/100 → 50점 (B)
```
- 부산 거주라 지역 만점이지만 캡션이 한국어(외국인-언어 축 0점)이고 음식 키워드가 적어 텍스트 축은 약했다.
- 도달이 24/40(배율 '보통' 1.20배 + 절대 1만~3만)으로 텍스트 축(26/60)보다 나아 **C→B로 올라갔다.**
- **근거가 항목별로 완전히 추적**된다.

**도달이 점수를 눌러 앉힌 경우** — `skzkxk333`(팔로워 6,719 → 소형):

```
조회수 추출 전:
[소형·조회수 미측정] 부산 22 + 한국 1 + 캡션 11 + 바이오 7 + 음식 17 = 58/60 → 97점 (B, 조회수 미측정이라 A→B)

조회수 추출 후(릴스 12개 중앙값 11,500회 · 팔로워 대비 1.71배):
[소형] 부산 22 + 한국 1 + 캡션 11 + 바이오 7 + 음식 17 + 배율 19 + 도달 12 = 89/100 → 89점 (A)
```
- 텍스트 축은 58/60으로 거의 완벽. 하지만 도달은 31/40이라 최종 89점.
- 미측정 상태에서는 97점이어도 **A를 주지 않는다** — 재보고 나서야 A가 확정된다.

---

## 6. PDF 원본 대비 미반영 (데이터 제약)

| 항목 | 상태 | 이유 |
|---|---|---|
| ④ 도달×신뢰 | ✅ **반영** | ER(평균 좋아요/팔로워) 대신 **릴스 조회수**로 구현(배율 + 절대 도달) |
| 지역 · 최근성 | ❌ 제외 | 게시물 날짜 미수집 (비어 있던 배점은 도달 축에 흡수) |
| 위치 기반 부산/한국 판별 | 🟡 키워드 근사 | 게시물 위치태그 미수집 |
| 외국인 축 | ⚠️ 언어 기준 | 국적이 아니라 **사용 언어**를 잼(한국어 쓰는 외국인은 낮게) — 국적 판단은 1차 선별이 담당 |

> - 도달 축은 원본의 ER(평균 좋아요/팔로워)을 그대로 쓰지 않는다. 좋아요는 여전히 미수집이고,
>   릴스 조회수가 **도달의 직접 측정치**라 프록시인 좋아요보다 낫다. 대신 조회수는 로그인한
>   브라우저에서만 보이므로 계정마다 수동 추출이 필요하다(전 계정 자동 채점이 아님).
> - 최근성까지 구현하려면 `discover.py`에 게시물 날짜 수집을 추가해야 함(크롤 요청량·차단 위험 증가).

---

## 7. 알려진 한계 / 다음 단계

- 캡션 수집이 기본 3개라 비율이 거칠다 → 정밀도를 높이려면 수집량 상향(크롤 비용↑).
- 지역·음식은 키워드 매칭이라 사진만 있고 텍스트가 없는 게시물은 놓칠 수 있음.
- 키워드 리스트는 지속 튜닝 대상.
